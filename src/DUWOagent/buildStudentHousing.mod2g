use "actions" as actionspec.
use "knowledge" as knowledge.

exit=always.


% Module to build student housing.
module buildStudentHousing {
	
	
	if bel(functions(FS), 
		member([Name,Id,L],FS),
		member('STUDENT',L)
		)  
			then building_plan_construction(Id, 1, square(10,10,200,10)).
}
