use actions as actionspec.
use demolishPredicates as knowledge.

order = linear.

module demolishMod {
	
	if bel(allBuildings('GARDEN', List), maxArea(List, BuildingID, Area),
		not(demolished(BuildingID)))
			then building_plan_demolish(BuildingID) + insert(demolished(BuildingID)).
}