use "knowledge" as knowledge.
use actions as actionspec.
use "goal" as goals.

exit=nogoals.

module DUWObuildRoadTest {
	% find a function to build a road (always 1 floor)
	if bel(functions(FS), member([Name,Id,L],FS), member('ROAD',L)) 
		then building_plan_construction(Id, 1, multipolygon("MULTIPOLYGON(((10 10, 200 10, 200 20, 10 20, 10 10)))")).
}
