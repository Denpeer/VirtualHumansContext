use predicates as beliefs.

module agentInit {
	if true
	then insert(settings([]))
		+ insert(functions([]))
		+ insert(stakeholders([]))
		+ insert(buildings([]))
		+ insert(zones([]))
		+ insert(filtered_percepts([])).
}
