use VirtualHumansContext as mas.
use "knowledge" as knowledge.
use main as module.
timeout = 5.

test tygron with
	pre{not(buildings(_))}
	in{ }
	post{buildings(_)}
	
	DUWOAgent {
		%do test_fail.
		
	}